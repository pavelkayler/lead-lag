# Bybit environment: demo | testnet | mainnet
BYBIT_ENV=demo

# Safety: for mainnet BYBIT_ENV, set 1 only if you really want real trading.
BYBIT_ALLOW_MAINNET=0

# API credentials (required for private WS / private REST / order.create)
BYBIT_API_KEY=
BYBIT_API_SECRET=

# --- Bybit endpoints ---
# You can keep defaults for selected BYBIT_ENV or override explicitly below.
# demo:
#   HTTP      https://api-demo.bybit.com
#   WS public wss://stream.bybit.com/v5/public/linear
#   WS private wss://stream-demo.bybit.com/v5/private
#   WS trade   (not supported in demo, backend uses REST)
# testnet:
#   HTTP      https://api-testnet.bybit.com
#   WS public wss://stream-testnet.bybit.com/v5/public/linear
#   WS private wss://stream-testnet.bybit.com/v5/private
#   WS trade  wss://stream-testnet.bybit.com/v5/trade
# mainnet:
#   HTTP      https://api.bybit.com
#   WS public wss://stream.bybit.com/v5/public/linear
#   WS private wss://stream.bybit.com/v5/private
#   WS trade  wss://stream.bybit.com/v5/trade

# Optional endpoint overrides (used by backend/src/exchange/bybitEnv.js)
BYBIT_HTTP_URL=https://api-demo.bybit.com
BYBIT_PUBLIC_WS_URL=wss://stream.bybit.com/v5/public/linear
BYBIT_PRIVATE_WS_URL=wss://stream-demo.bybit.com/v5/private
# In demo keep empty; for testnet/mainnet set explicit trade WS URL if needed.
BYBIT_TRADE_WS_URL=

# Position mode for order.create: hedge | oneway | auto
BYBIT_POSITION_MODE=hedge

# Trading gate (must be 1 to allow any order.create calls)
ENABLE_TRADING=0
MAX_NOTIONAL_USDT=50

# Logger
LOG_DIR=./logs

# Risk/ops
ORDER_TIMEOUT_MS=15000
MAX_OPEN_ORDERS=10

# CoinMarketCap (optional): used to filter universe by market cap >= 10m
CMC_API_KEY=your_cmc_key_here

# Paper fees/slippage (bps per side)
PAPER_FEE_BPS=6
PAPER_SLIPPAGE_BPS=2

# Paper test runner
# TRADE_STATE_POLL_MS=2500

# Public WS symbols cap for test runs
MAX_SYMBOLS=30
