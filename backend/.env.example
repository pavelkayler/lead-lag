# Bybit Demo Trading (no testnet)
# Docs: https://bybit-exchange.github.io/docs/v5/demo
# REST: https://api-demo.bybit.com
# WS private: wss://stream-demo.bybit.com/v5/private
# Public market data is mainnet: wss://stream.bybit.com/v5/public/linear (no key needed)

BYBIT_ENV=demo
# Position mode for order.create: hedge | oneway | auto
BYBIT_POSITION_MODE=hedge
BYBIT_ALLOW_MAINNET=0

# Demo Trading API Key/Secret (create inside Bybit -> Demo Trading -> API)
BYBIT_API_KEY=
BYBIT_API_SECRET=

# Public market REST (no key needed). Keep mainnet for reference.
BYBIT_PUBLIC_HTTP_URL=https://api.bybit.com

# Trading gate (must be 1 to allow any order.create calls)
ENABLE_TRADING=0
MAX_NOTIONAL_USDT=50

# Logger
LOG_DIR=./logs

# Risk/ops
ORDER_TIMEOUT_MS=15000
MAX_OPEN_ORDERS=10
# Position mode for order.create: hedge | oneway | auto
BYBIT_POSITION_MODE=hedge

# CoinMarketCap (optional): used to filter universe by market cap >= 10m
CMC_API_KEY=your_cmc_key_here

# Paper fees/slippage (bps per side)
PAPER_FEE_BPS=6
PAPER_SLIPPAGE_BPS=2

# Paper test runner
# TRADE_STATE_POLL_MS=2500

# Public WS symbols cap for test runs
MAX_SYMBOLS=30
